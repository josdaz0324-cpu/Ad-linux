# /etc/pam.d/common-account
# Enforces centralized SSSD account rules and domain logon windows

account [success=1 new_authtok_reqd=done default=ignore] pam_unix.so
account [success=ok authinfo_unavail=ignore user_unknown=ignore default=bad] pam_sss.so
account required                        pam_permit.so
